spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jobmatch?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 3veni333
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # dialect auto-detected for MySQL
  flyway:
    enabled: true
    locations: classpath:db/migration
  threads:
    virtual:
      enabled: true   # Java 21 virtual threads

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

# Default scoring weights — overridable via query params
scoring:
  weights:
    skill: 50
    experience: 20
    location: 15
    salary: 15
